---
description: Rustの実装計画・設計について、grill-meスキルを用いてユーザーと共通の認識に達するまで質問を投げかける。「具体的にこうしたい」という計画は詳細まで詰め、「ここがあんまり、どうすればいいか」という困りごとは調査した上で改善案を提案する。コード参照が不要な純粋な設計相談は /think を使うこと。決定事項は最終的に /impl-rust にそのまま渡せる仕様書としてまとめる。
argument-hint: <計画・設計のテーマ | 困りごと・相談内容> [対象パス]
# 決定事項ファイルの設計時刻取得にのみ Bash を使う
allowed-tools: Skill(grill-me), Read, Glob, Grep, AskUserQuestion, Write, Bash(date *)
---

# Rust計画のグリル

**テーマ**: $ARGUMENTS

> - **具体的な計画・方針の指定**: それを叩き台にして詳細を詰める（例:「〇〇機能を△△という設計で追加したい」）
> - **困りごと・相談内容の指定**: それを起点に調査し、改善案を練る（例:「エラーハンドリングが場当たり的で辛い」「これはこうしたほうがいい気がする、どう思う？」）
> - **対象パスの指定**: そのディレクトリ・ファイルを調査の起点にする
> - **未指定**: 直前の会話コンテキストを起点にする

## 手順

1. `grill-me` スキルをテーマ（と対象パス）を引数にして呼び出し、共通認識に達するまで議論する。その際、grill-meの原則に加えて以下をRust固有の原則として適用する。
   - **実装・コード変更は行わない** — 論点をすり合わせ、決定事項をまとめるところまで。実装は `/impl-rust` に引き継ぐ
   - **根本原因を論点にする** — 困りごとが起点のときは表面の症状ではなく根本原因を論点にする（例:「コメントが多すぎる」の裏に「命名が意図を表現できていない」があればそちらを論点にする）
   - **事実と意見を区別する** — コードの現状は `ファイル名:行番号` で根拠を示し、価値判断と混ぜない
2. 調査の過程で、この計画・相談とは別に原因不明の既存バグを発見した場合は、その場で深追いせず `/thought-bug` の使用を提案する。
3. grill-meの議論を経て全論点について共通の認識（または仮定の承認）に達したら、下記「決定事項のまとめ」に従って仕様書を書き出す。

## 決定事項のまとめ

決定事項をMarkdownファイルにまとめて、claude-code起動時のカレントディレクトリ直下に書き出す。このファイル単体を渡せば別セッション・別コンテキストからでも作業に着手できるように、以下を含めること。

- 設計時刻: `date '+%Y-%m-%d %H:%M:%S'` を実行して取得した現在時刻を `yyyy-mm-dd HH:MM:SS` 形式でファイル冒頭に記載する
- 調査対象: 何についての調査・設計かを明記する
- 概要
- 結論
- 合意した設計・決定事項とその理由
- 実装に必要な調査結果・コンテキスト（根拠となった `ファイル名:行番号` を含む）
- 参照すべきファイル・パス
- 残っている未解決点（あれば）

さらに、この仕様書はあくまでtemporaryな一時的agent指示ファイルであることを明記する。多くの場合これはAIへの指示を目的としたものであり、ドキュメントやコードコメントで説明すべき内容ではないためである。また、記載した設計時刻から時間が経過している場合、実際の実装や仕様が本書の内容と異なっているケースがありうる旨も明記する。

この仕様書は `/impl-rust` にそのまま渡せる形式で書く（実装方針・コメント方針は `rust-coder` スキルに準拠する前提で書く）。
